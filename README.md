####Base64decoder JS application

A simple online tool for decoding string

###Install and run

```npm install``` to install application dependencies

To run local server

```npm install webpack -g```
```npm install webpack-dev-server -g```